RewriteEngine On
RewriteBase /slovakia-tour/public/

# Existujúce súbory/adresáre nepretrasujeme (css, js, img)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Všetko ostatné → front controller
RewriteRule ^ index.php [L]
